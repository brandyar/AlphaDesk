# راهنمای جامع دیپلوی روی کولیفای (Coolify Deployment Guide)

این پروژه برای استقرار خودکار و سریع در پنل **Coolify** با استفاده از معماری Multi-stage Docker بهینه‌سازی شده است.

---

### ۱. روش‌های استقرار در Coolify

در داشبورد کولیفای می‌توانید به دو روش پروژه را اضافه کنید:

#### روش اول: گیت‌هاب / گیت‌لب (توصیه شده - Git Repository)
1. در پنل Coolify به مسیر **Projects** رفته و پروژه مورد نظر را باز کنید.
2. روی **+ New** و سپس **Application** کلیک کنید.
3. مخزن گیت (GitHub/GitLab/Custom Git) خود را انتخاب کنید.
4. نوع بیلد (Build Pack) را روی **Dockerfile** قرار دهید (کولیفای به صورت خودکار `Dockerfile` ریشه پروژه را شناسایی می‌کند).

#### روش دوم: داکر کامپوز (Docker Compose)
1. در هنگام ساخت اپلیکیشن در Coolify، گزینه **Docker Compose** را انتخاب کنید.
2. کولیفای به صورت خودکار فایل `docker-compose.yml` موجود در مخزن را خوانده و سرویس را بالا می‌آورد.

---

### ۲. متغیرهای محیطی در Coolify (Environment Variables)

در تب **Environment Variables** اپلیکیشن در Coolify، متغیرهای زیر را تنظیم کنید:

| نام متغیر | مقدار پیش‌فرض / پیشنهادی | توضیحات |
| :--- | :--- | :--- |
| `NODE_ENV` | `production` | فعال‌سازی حالت پروداکشن و بهینه‌سازی‌ها |
| `PORT` | `3000` | پورت داخلی سرور Express |
| `DIRECTUS_URL` | `http://directus:8055` یا آدرس سرور شما | آدرس اینستنس دایرکتوس (در شبکه داکر یا اینترنت) |
| `DIRECTUS_ADMIN_TOKEN` | `your-static-admin-token` | توکن استاتیک ادمین برای درخواست‌های BFF |

> **نکته مهم**: اگر دایرکتوس شما نیز روی همین سرور Coolify قرار دارد، می‌توانید از نام سرویس داخلی داکر (مثلاً `http://directus:8055`) استفاده کنید تا ترافیک امن و درون‌شبکه‌ای بماند.

---

### ۳. تنظیمات شبکه و دامنه در Coolify

- **Domains / FQDN**: در بخش Domains کولیفای، دامنه خود را وارد کنید (مثال: `https://crm.yourcompany.com`). کولیفای به صورت خودکار گواهینامه SSL رایگان (Let's Encrypt) صادر می‌کند.
- **Port Mapping**: پورت کانتینر `3000` است (کولیفای ترافیک دامنه را به پورت `3000` کانتینر فوروارد می‌کند).
- **Healthcheck Path**: آدرس سلامت‌سنجی کانتینر روی `/api/health` تنظیم شده است و کانتینر به صورت خودکار پایش می‌شود.

---

### ۴. معماری و چرخه حیات بیلد (Build Lifecycle)

1. **Stage 1 (Builder)**: نصب وابستگی‌ها با `npm install --legacy-peer-deps` و کامپایل بیلد فرانت‌اند ری‌اکت به دایرکتوری `dist/`. سپس پالایش پکیج‌ها با `npm prune --omit=dev`.
2. **Stage 2 (Runner)**: بر پایه ایمیج رسمی `node:20-alpine` بدون هیچ‌گونه نیاز به دانلود بسته‌های سیستم‌عامل (`apk`/`apt`)، کپی وابستگی‌ها و فایل‌ها برای کاربر ایزوله سیستم `node`، و پایش خودکار سلامت با تابع `fetch` پیش‌فرض Node.js.
3. سرور BFF بر روی پورت `3000` اجرا شده و هم درخواست‌های API را پاسخ می‌دهد و هم فرانت‌اند SPA را با بالاترین سرعت لود می‌کند.
